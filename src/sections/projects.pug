
section.row#Projects
    h2 Projects

    each project in projects
        .row
            .one.column(style="width: 3%")
                i.nf(class=project.icon)
            .eight.columns.itemName= project.name
            .three.columns.date= project.date
        .row
            .one.column
            .eleven.columns.description= project.description
